# 다른 환경에서 이어서 작업하기

이 압축본은 2026-08-08 시점의 전체 작업물이다. `.git` 이력이 들어 있으므로
커밋 메시지로 «왜 그렇게 했는지»까지 따라갈 수 있다.

**먼저 [HANDOVER.md](HANDOVER.md) 를 읽을 것.** 공연 전 반드시 해야 할 작업이 남아 있다.

---

## 1. 압축 풀고 시작하기

```bash
cd malmoi-play
npx serve . -l 4321      # http://localhost:4321
```

원격 저장소는 <https://github.com/new-generation25/malmoi-play> 이고 `main`에
푸시하면 Vercel(<https://malmoi-play.vercel.app>)이 자동 재배포한다.
다른 배포처를 쓸 거라면 **정적 호스팅이면 무엇이든 된다**(빌드 설정 불필요).

`demo.html`은 서버 없이 더블클릭만으로 도는 단독 실행본이다(가상 관객 모드).
`build.js`로 다시 만들 수 있으므로 git에는 추적하지 않는다.

---

## 2. 지금 상태 한 줄 요약

| 항목 | 상태 |
| --- | --- |
| 무대/관객폰 화면 분리 | ✅ 완료 |
| DM 채팅 연출 (`dm.html`) | ✅ 완료 |
| 진행자 개입 (7/9/0/A/E/H) | ✅ 완료 |
| Supabase 코드 연동 | ✅ 완료 |
| **Supabase 테이블 생성** | ❌ **미완 — SETUP.md 2단계 SQL 실행 필요** |
| 실제 폰 QR 스캔 리허설 | ❌ 미확인 |

---

## 3. 이미 내린 결정 (다시 검토하지 않아도 되는 것)

새 세션에서 같은 논의를 반복하지 않도록 결정 사항과 이유를 남긴다.

- **문제/선택지를 무대에 띄우지 않는다.** 관객마다 접속 시점과 푸는 속도가 달라
  "모두가 3번 문제를 푸는 중" 같은 동기화된 상태가 존재하지 않는다. 무대는
  집단의 진척도(게이지)만 보여준다.
- **WebSocket이 아니라 1초 폴링.** 공연은 단 한 번뿐이라, 이벤트 유실 시 게이지가
  영구히 틀어지는 것보다 매번 총계를 다시 읽어 스스로 복구되는 편이 안전하다.
- **«목표 대비 비율»을 쓰지 않는다.** 관객 수를 미리 알 수 없다. 응답이 쌓일수록
  상승폭이 줄며 90%에 수렴하는 곡선을 쓴다. 30명이든 300명이든 게이지가 거꾸로
  가거나 도중에 멈추지 않는다.
- **진행자 트리거에 조건을 걸지 않는다.** 예전에 «75% 이상일 때만 급습 가능» 제한을
  뒀다가, 정작 공연에서 눌러도 반응이 없는 것처럼 보였다. 연출 도구는 원하는 순간
  반드시 반응해야 한다.
- **클릭 단계를 게이지 상태에서 추론하지 않는다.** 추론하던 시절 A(자동 복귀) 이후
  클릭이 종료 대신 급습으로 튀어, 진행자가 장면을 못 끝내는 버그가 있었다.
  지금은 `clickStep`으로 따로 센다.
- **publishable key는 공개가 정상.** 브라우저 노출 전제로 설계된 키이고 권한은
  RLS가 통제한다. `secret`(구 service_role) 키는 절대 넣지 말 것.

---

## 4. 알아두면 헷갈리지 않는 것

- `index.html` 한 파일이 **무대(`/`)와 관객폰(`/?p=1`) 두 모드**를 겸한다.
  `PHONE_ONLY` 분기와 `body.stage` / `body.phone-only` 클래스로 갈린다.
- `malmoi-artifact.html`은 Artifact 배포용 산출물이라 **웹 배포에는 쓰지 않는다.**
  Artifact 환경은 외부 통신을 막으므로 거기서는 Supabase 실집계가 동작하지 않는다.
- 문제를 바꾸려면 `index.html`의 `QUESTIONS` 배열만 고치면 무대·폰 양쪽에 반영된다.
- `localhost`로 열면 QR도 `localhost`를 가리켜 관객 폰에서 열리지 않는다.
  QR 테스트는 반드시 배포 주소에서 한다.

---

## 5. 문서 지도

| 문서 | 용도 |
| --- | --- |
| `HANDOVER.md` | **인수인계 — 가장 먼저 읽을 것.** 미완 작업·조작법·검증 현황·위험 |
| `SETUP.md` | Supabase 설정 (테이블 생성 SQL 포함) |
| `STAGE-SCREEN.md` | 무대 스크린 설계 문서. 다른 프로젝트로 이식할 때 |
| `README.md` | 운영자용 사용 설명 |
