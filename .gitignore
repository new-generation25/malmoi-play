# Claude Code 로컬 설정(개인 권한 설정이라 공유하지 않는다)
.claude/settings.local.json

# Vercel CLI
.vercel

node_modules/

# 연출 디자인 원본(배포 불필요한 대용량 바이너리)
*.pptx

# OS
Thumbs.db
desktop.ini
.DS_Store

# 단독 실행 데모(빌드 산출물)
demo.html
