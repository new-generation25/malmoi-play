# 퀴즈 무대 스크린 — 설계 문서

공연 무대에 띄우는 «말모이 원고 복원율» 게이지 화면. 이 문서 하나만 읽으면
다른 프로젝트에서 같은 연출을 재현하거나 이 코드를 옮겨 쓸 수 있도록 정리했다.

- **파일**: `index.html` (단일 파일. 무대/관객폰 두 모드를 한 파일에 담음)
- **로컬 주소**: <http://localhost:4321/index.html>
- **배포 주소**: <https://malmoi-play.vercel.app/>

로컬 실행:

```bash
npx serve . -l 4321
```

---

## 1. 이 화면이 하는 일

관객이 QR로 접속해 퀴즈를 푸는 공연 이벤트에서, **무대 대형 스크린에 띄우는 화면**이다.
문제와 선택지는 **일부러 띄우지 않는다.** 관객마다 접속 시점과 푸는 속도가 다르므로
"모두가 3번 문제를 푸는 중" 같은 동기화된 상태가 존재하지 않기 때문이다.

대신 무대는 **집단의 진척도**만 보여준다.

```
┌──────────────────────────────────────────────┐
│ 말 모 이  작 전            문제 진행 중       │  ← 상단바
│ 접속 관객 50명 ▓▓▓▓▓▓▓▓▓░  응답 312          │  ← 접속/응답 카운터
│                                              │
│      "천지빼까리다"    "꼬시다"               │  ← 말풍선이 떠올랐다 사라짐
│           "파이다"                            │
│                                              │
│                  70                          │  ← 큰 퍼센트 숫자
│                   %                          │
│         말 모 이  원 고  복 원 율             │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░│목표 100%   │  ← 메인 게이지
│  0%      관객의 응답을 모으는 중…      100%   │
└──────────────────────────────────────────────┘
```

---

## 2. 핵심 설계 규칙 4가지

이 화면의 성격을 결정하는 규칙들. **연출 목적이 기술적 정확성보다 우선한다.**

### 규칙 1 — 100%에 절대 닿지 않는다 (`MAX_PCT = 90`)

모든 응답이 도착해도 게이지는 **90%에서 멈춘다.** 목표선 100%는 화면에 보이지만
닿지 않는다. "나머지 10%는 여러분의 입으로 이어 주십시오"라는 극의 메시지를 위한
의도적 설계다. 진행률 표시가 아니라 **연출 장치**다.

### 규칙 2 — 순사 급습: 진행자가 원하는 순간 1회 (`DROP_PCT = 70`)

무대 화면을 클릭하면 게이지가 **70%로 떨어지고** 화면이 흔들리며 "순사 급습!"이 뜬다.
공연 중 **1회만** 발동한다.

초기 구현에는 "게이지 75% 이상일 때만 발동" 제한이 있었으나 **제거했다.** 게이지가
75%를 넘는 건 25~30초 진행 중 마지막 몇 초뿐이라, 진행자가 원하는 타이밍에 눌러도
아무 반응이 없는 것처럼 보였기 때문이다. 연출 도구는 진행자가 통제할 수 있어야 한다.

단, 아직 70%에 못 미친 시점에 누르면 70%로 **올라가는** 모순이 생긴다.
그래서 그 경우엔 **현재 값의 75% 지점**으로 떨어뜨린다 — 언제 눌러도 항상 내려간다.

### 규칙 3 — 방해를 당해도 결말은 항상 90%

핵심 트릭이다. 방해 시점을 기록해 두고, **남은 응답이 `dropTo → 90%` 구간을 다시
채우도록 구간을 재매핑**한다. 덕분에 방해가 있든 없든, 언제 방해했든 마지막은 90%다.

```js
const realPct = () => answered / EXPECTED * MAX_PCT;   // 실제 진척도

function shownPct() {                                   // 화면에 보이는 값
  const p = realPct();
  if (!sabotaged) return p;
  if (sabotageAt >= MAX_PCT) return MAX_PCT;            // 0으로 나누기 방지
  return dropTo + (p - sabotageAt) * (MAX_PCT - dropTo) / (MAX_PCT - sabotageAt);
}
```

검증된 동작:

| 클릭 시점 | 하락 후 | 최종 |
| --- | --- | --- |
| 22.5% | 16.9% | 90% |
| 54% | 40.5% | 90% |
| 81% | **70%** | 90% |
| 88.9% | **70%** | 90% |

### 규칙 4 — 서버가 없다 (중요)

**관객 폰의 응답은 무대 스크린에 전달되지 않는다.** 두 화면은 완전히 독립적이다.
무대 게이지를 움직이는 것은 **시뮬레이션된 가상 관객 50명**뿐이다. 관객이 100명이든
0명이든 무대 게이지는 똑같이 올라간다.

> 즉 이 화면은 **관객 참여가 무대에 반영되는 것처럼 보이는 연출**이다.
> 실제 실시간 집계가 필요하면 서버 + 실시간 DB(Supabase 등)가 별도로 필요하다.

---

## 3. 상태 모델

```
lobby ──(무대 화면 클릭)──> play ──(응답 400개 도달)──> end
                             │                          │
                    (화면 클릭: 순사 급습 1회)      (클릭: 리셋)
```

| 변수 | 의미 |
| --- | --- |
| `phase` | `"lobby"` / `"play"` / `"end"` |
| `answered` | 지금까지 도착한 총 응답 수 (0 → 400) |
| `joined` | 접속 관객 수 (로비에서 0 → 50) |
| `sabotaged` | 순사 급습 발동 여부 (1회 제한용) |
| `sabotageAt` | 방해 시점의 **실제** 복원율 (재매핑 기준점) |
| `dropTo` | 방해로 떨어질 지점 (보통 70, 초반이면 현재값×0.75) |

### 주요 상수

| 상수 | 값 | 의미 |
| --- | --- | --- |
| `TOTAL` | 50 | 시뮬레이션할 가상 관객 수 |
| `EXPECTED` | 400 | `TOTAL × 문제수(8)` — 종료 조건 |
| `MAX_PCT` | 90 | 게이지 상한 (100%에 닿지 않음) |
| `DROP_PCT` | 70 | 순사 급습 시 하락 지점 |
| `BUBBLE_MAX` | 14 | 동시 표시 말풍선 수 (400개 다 띄우면 렌더링이 죽는다) |

---

## 4. 가상 관객 시뮬레이션

**동기화하지 않는 것**이 핵심이다. 50명 각자에게 독립적인 타임라인을 깔아 준다.

```js
function scheduleCrowd() {
  for (let p = 0; p < TOTAL; p++) {           // 50명 전원
    let t = 300 + Math.random() * 2500;       // 각자 시작 시점이 다름
    for (let k = 0; k < QUESTIONS.length; k++) {
      t += 700 + Math.random() * 2600;        // 문제당 걸리는 시간도 제각각
      const q = QUESTIONS[k];
      later(() => {
        const hit = Math.random() < 0.62;     // 정답률 62%
        addAnswer(hit ? correctTextOf(q) : wrongTextOf(q), hit);
      }, t);
    }
  }
}
```

- 전체 소요: **약 25~30초**에 걸쳐 400개 응답이 도착
- 정답률 62%로 가중 (정답이 더 많이 뜨되 오답도 섞임)
- 말풍선에는 실제 응답 텍스트가 찍힘 ("천지빼까리다", "꼬시다" 등)

> ⚠️ **함정**: 초기 구현은 `TOTAL - 1`(49명)만 돌렸다. 남은 8개 응답은 진행자가
> 무대에 붙은 폰 목업으로 직접 풀어야 채워졌고, 안 풀면 게이지가 **88.2%에서
> 영영 멈췄다.** 반드시 `TOTAL` 전원을 돌려 `EXPECTED`에 정확히 도달시켜야 한다.

---

## 5. 시각 효과

| 요소 | 구현 |
| --- | --- |
| **말풍선 뿅** | 응답 1건마다 아래에서 떠올라 2.6초간 상승하며 사라짐. 정답은 금색(`.hit`) |
| **게이지 상승** | `cubic-bezier(.34,1.3,.64,1)` — 살짝 튕기는 느낌 |
| **순사 급습** | 게이지 붉게 변색 + 화면 흔들림(`shake` 0.45s) + 전체 오버레이 + 경보음 |
| **효과음** | Web Audio API로 **합성** (외부 파일 없음). 응답 도착 tick / 정답 팡파레 / 경보 |
| **접속 카운터** | 로비에서 60~150ms 간격으로 1명씩 증가하며 짧은 beep |

말풍선은 45ms 이내 중복 beep을 억제한다(400건이 몰리면 소리가 뭉개짐).

---

## 6. 한 파일 두 모드 구조

`index.html` 하나가 **URL 쿼리로 무대/관객폰을 분기**한다.

```js
const PHONE_ONLY = new URLSearchParams(location.search).get("p") === "1";
const JOIN_URL = BASE_URL + "?p=1";   // QR이 가리키는 주소
```

| 주소 | 모드 | body 클래스 | 동작 |
| --- | --- | --- | --- |
| `/` | 무대 | `stage` | 폰 목업 숨김, 가상 관객 실행, 순사 급습 가능 |
| `/?p=1` | 관객 폰 | `phone-only` | 무대 스크린 숨김, 퀴즈만 전체화면, 시뮬레이션·급습 없음 |

관객 폰에서 `screen.onclick` 맨 앞에 `if (PHONE_ONLY) return;`을 두어 **관객이 순사
급습 연출을 발동시키지 못하게** 막는다.

> QR은 반드시 `?p=1`을 인코딩해야 한다. 그렇지 않으면 관객 폰에도 무대 스크린이
> 같이 떠서, 관객마다 자기만의 게이지를 보게 되고 무대 숫자와 어긋난다.

---

## 7. 다른 프로젝트로 옮길 때 체크리스트

1. **`EXPECTED`에 정확히 도달하는가** — 시뮬레이션이 종료 조건에 못 미치면 영영 안 끝난다
2. **`shownPct()` 재매핑이 항상 상한값으로 수렴하는가** — 방해 시점과 무관하게 결말이 같아야 한다
3. **`sabotageAt >= MAX_PCT` 가드** — 없으면 0으로 나누기가 발생한다
4. **말풍선 개수 상한** — 없으면 응답이 몰릴 때 DOM이 폭발한다
5. **진행자 트리거에 조건을 걸지 말 것** — 연출 도구는 원하는 순간 반드시 반응해야 한다
6. **오디오는 첫 사용자 조작 이후 켜진다** — 브라우저 정책. 시작 클릭에서 잠금 해제

---

## 8. 관련 파일

| 파일 | 설명 |
| --- | --- |
| `index.html` | 이 문서가 설명하는 본체 |
| `dm.html` | 같은 공연의 다른 장면(덕이↔아란 DM 채팅 연출). 완전 독립 |
| `qrcode.js` | QR 생성 (Kazuhiko Arase, MIT) |
| `build.js` | Artifact 배포용 단일 파일 병합 스크립트 |
| `README.md` | 공연 운영자용 사용 설명 |
