# 인수인계 — 말모이 작전 공연용 웹 연출

작성일 2026-08-07 · 저장소 <https://github.com/new-generation25/malmoi-play>

공연 중 관객이 QR로 접속해 퀴즈를 풀고, 그 응답이 극장 무대 스크린의 게이지에
실시간으로 반영되는 웹 페이지 묶음이다.

---

## 🔴 가장 먼저 — 공연 전 반드시 해야 할 일

**Supabase 테이블이 아직 생성되지 않았다. 이 상태로는 실시간 집계가 동작하지 않는다.**

무대 화면 하단 진행자 바에 빨간 **«집계 끊김 N회»** 가 뜨고 있다면 이 작업이 안 된 것이다.

1. <https://supabase.com/dashboard/project/mxsxkktgsmaumeugripe/sql/new> 접속
2. [SETUP.md](SETUP.md) **2단계 SQL 블록**을 통째로 붙여넣고 **Run**
3. 무대 화면을 새로고침 → 진행자 바가 초록 **«집계 연결됨»** 으로 바뀌는지 확인

> 왜 대신 해두지 않았나: 테이블 생성(DDL)은 REST API로 불가능하고, Supabase CLI
> 로그인은 대화형이며, DB 직접 접속에는 비밀번호가 필요하다. 비밀번호를 주고받지
> 않는 편이 안전해 SQL Editor 실행만 남겨 두었다. 30초면 끝난다.

이후 리허설에서 **폰으로 QR을 직접 찍어** 무대의 접속·응답 숫자가 오르는지 반드시 확인할 것.

---

## 1. 페이지 구성

배포는 Vercel 정적 호스팅. `main` 브랜치에 푸시하면 자동 재배포된다.

| 주소 | 화면 | 누가 |
| --- | --- | --- |
| <https://malmoi-play.vercel.app/> | 퀴즈 **무대 스크린** (게이지) | 진행자 — 극장 프로젝션 |
| <https://malmoi-play.vercel.app/?p=1> | 퀴즈 **관객 폰** | 관객 — QR로 접속 |
| <https://malmoi-play.vercel.app/dm.html> | **덕이 ↔ 아란 DM 채팅** 연출 | 진행자 — 다른 장면 |

세 페이지는 서로 링크로 이어져 있지 않다. 진행자가 장면마다 URL을 직접 연다.
무대 화면의 QR은 자동으로 `?p=1`을 인코딩하므로 관객은 스캔만 하면 된다.

---

## 2. 공연 중 조작법

### 퀴즈 무대 스크린

화면 하단에 진행자 바가 있다(접속·응답·게이지·연결상태 표시). 관객석에 노출되면 **H** 로 숨긴다.

| 조작 | 동작 |
| --- | --- |
| **클릭 / Space** | 한 단계씩: 작전 시작 → 순사 급습 → 작전 완료 → 리셋 |
| **1** | 의견 1건 — 게이지를 1%씩 올린다 (말풍선·소리까지 진짜 응답처럼) |
| **7** | 순사 급습 — 게이지를 70%로 떨어뜨리고 거기서 다시 상승 |
| **9** | 게이지 90% 고정 |
| **0** | 게이지 100% — 축포가 터지고 1초 뒤 작전 완료 화면으로 (관객 폰도 같이) |
| **A** | 개입 해제 — 실집계 값으로 복귀 |
| **H** | 진행자 바 숨김/표시 |

게이지는 평소 관객 응답을 따라 오르고, 연출이 필요한 순간에만 위 키로 직접 잡는다.

### DM 채팅 (`/dm.html`)

| 조작 | 동작 |
| --- | --- |
| 클릭 / Space / → | 다음 대사 (타이핑 중이면 즉시 완성) |
| ← / Backspace | 뒤로 |
| R | 처음부터 |
| F | 전체화면 |
| `+` / `−` | 타이핑 속도 |
| `[` / `]` | 음량 |
| H | 진행자 힌트 숨김 |

효과음은 코드로 합성한다(외부 음원 파일 없음). **첫 클릭 이후에 소리가 켜진다** —
브라우저 정책이라 정상이며, 시작 클릭 한 번으로 해제된다.

---

## 3. 구조

```
관객 폰 (/?p=1)  ──POST──▶  Supabase  ◀──1초 폴링──  무대 스크린 (/)
```

- **Supabase 프로젝트**: `mxsxkktgsmaumeugripe` (URL·publishable key는 `index.html` 상단)
- **테이블 2개**: `participants`(접속 기기), `answers`(응답)
- 폰은 접속 시 기기ID를 등록하고, 문제를 풀 때마다 응답을 POST 한다
- 무대는 1초마다 새 응답을 증분 조회하고 접속 인원을 센다

### 왜 WebSocket이 아니라 폴링인가

공연은 단 한 번뿐이다. WebSocket은 이벤트가 한 번 유실되면 게이지가 **영구히** 틀어지지만,
폴링은 매번 총계를 다시 읽어 **스스로 복구**된다. 무대 게이지에서 1초 지연은 보이지 않는다.

### 게이지 계산

관객 수를 미리 알 수 없으므로 «목표 대비 비율»을 쓰지 않는다. 응답이 쌓일수록 상승폭이
줄며 90%에 수렴하는 곡선이다. 관객이 30명이든 300명이든 **거꾸로 가거나 도중에 멈추지 않는다.**

| 참여 규모 | 게이지 |
| --- | --- |
| 10명 × 8문제 | 55% |
| 30명 × 8문제 | 74% |
| 100명 × 8문제 | 85% |
| 200명 × 8문제 | 87% |

100%에는 자연히 닿지 않는다("나머지는 여러분의 입으로"라는 극의 메시지). 필요하면 **0** 키로 100% 고정.

---

## 4. 파일

| 파일 | 설명 |
| --- | --- |
| `index.html` | 퀴즈 본체. `?p=1` 유무로 무대/폰 두 모드 분기 |
| `dm.html` | DM 채팅 연출. 완전 독립 실행 |
| `qrcode.js` | QR 생성 라이브러리 (Kazuhiko Arase, MIT) |
| `build.js` | `malmoi-artifact.html` 생성 스크립트 |
| `malmoi-artifact.html` | Artifact 배포용 단일 파일. **웹 배포에는 안 쓰임** |
| `README.md` | 운영자용 사용 설명 |
| `SETUP.md` | Supabase 설정 (SQL 포함) |
| `STAGE-SCREEN.md` | 무대 스크린 설계 문서 (다른 프로젝트 이식용) |

`덕이아란_DM.pptx`(디자인 원본)는 대용량 바이너리라 `.gitignore`로 제외했다. 로컬에만 있다.

### 로컬 실행

```bash
npx serve . -l 4321      # http://localhost:4321
node build.js            # malmoi-artifact.html 재생성
```

> `localhost`로 열면 QR도 `localhost`를 가리켜 관객 폰에서는 열리지 않는다.
> QR 테스트는 반드시 배포 주소에서 할 것.

---

## 5. 검증된 것 / 안 된 것

### 검증 완료

- 무대/폰 화면 분리, 폰에서 8문제 진행, 관객이 연출 키를 못 쓰는 것
- 게이지 수식 — 관객 규모별 단조 증가, 개입 후에도 90% 초과 없음
- 진행자 키 7/9/0/A, 클릭 진행 순서, 순사 급습 연출
- **Supabase REST 왕복** — 실제 Supabase를 흉내낸 로컬 목 서버로
  폰 접속→등록→무대 카운터, 응답 8건→게이지 상승→말풍선까지 전 구간 확인
- DM 페이지 7개 대사 재생, 뒤로/처음 조작
- 세 페이지 모두 배포 반영(라이브 = 로컬 바이트 동일), 콘솔 에러 없음

### 아직 확인 못 한 것 ⚠️

| 항목 | 사유 |
| --- | --- |
| **실제 Supabase 왕복** | 테이블 미생성. 위 «가장 먼저» 항목 실행 후 확인 필요 |
| **실제 폰에서 QR 스캔** | 브라우저 도구로는 카메라 스캔 불가. 리허설에서 직접 |
| **효과음 실제 소리** | 도구로 오디오 출력 캡처 불가. 코드 호출은 확인됨 |
| **다수 동시 접속 부하** | 관객 수십~수백 명 동시 접속은 미검증 |

---

## 6. 알려진 제약·위험

- **네트워크 의존**: 극장 와이파이가 불안정하면 집계가 끊긴다. 진행자 바에 «집계 끊김»이
  뜨면 바로 알 수 있고, 이때는 **7/9/0 키로 게이지를 수동 진행**하면 공연은 계속된다.
  이것이 진행자 개입 기능을 넣은 실질적 이유이기도 하다.
- **응답 조작 가능**: 익명 접속이라 누군가 반복 제출할 수 있다. 다만 게이지는
  진행자가 언제든 덮어쓸 수 있어 공연 진행에는 지장이 없다.
- **회차 데이터 혼입**: 여러 번 공연하면 이전 데이터가 남는다. `?s=회차명`으로 분리하거나
  공연 전 데이터를 비운다(SETUP.md 참고). 단, 무대 QR은 `s` 값을 물려주지 않으므로
  회차를 나눌 땐 관객용 QR을 따로 만들어야 한다.
- **`malmoi-artifact.html`은 Supabase에 못 붙는다**: Artifact 환경은 외부 통신을 막는다.
  웹 배포본(`index.html`)만 실집계가 동작한다.
- **publishable key는 공개가 정상**: 브라우저 노출 전제로 설계된 키이고 권한은 RLS가
  통제하므로 저장소에 그대로 있다. **`secret`(구 service_role) 키는 절대 넣지 말 것** —
  모든 보안 정책을 우회한다.

---

## 7. 다음에 손볼 만한 것

- 무대 QR에 회차(`s`) 값을 자동으로 물려주기 — 지금은 회차 분리 시 QR을 따로 만들어야 함
- 접속 인원 눈금이 50명 고정 — 대규모 공연이면 눈금 조정 필요 (`TOTAL` 상수)
- 문제 교체는 `index.html`의 `QUESTIONS` 배열만 수정하면 된다(무대·폰 양쪽에 자동 반영)
