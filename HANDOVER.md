# 인수인계 — 말모이 작전 공연용 웹 연출

최종 확인 2026-08-11 · 저장소 <https://github.com/new-generation25/malmoi-play>

「King받은 이도 : 한글 폼 미쳤다!」(2026.8.16~8.23 회현동소극장) 공연에서 쓰는
웹 연출 페이지 묶음이다. 관객이 QR로 접속해 퀴즈를 풀면 그 응답이 무대 스크린
게이지에 실시간으로 반영된다.

> 다른 환경에서 개발을 이어받는 경우 [CONTINUE.md](CONTINUE.md) 를,
> 대본 TTS 음성 작업은 [TTS-VOICE.md](TTS-VOICE.md) 를 함께 볼 것.

---

## 1. 지금 상태

| 항목 | 상태 |
| --- | --- |
| 무대/관객폰 화면 분리 | ✅ |
| Supabase 실시간 집계 | ✅ **테이블 생성 완료, 실데이터 확인됨** |
| Q-Studio 임베드 연동 | ✅ |
| 진행자 개입 키 | ✅ |
| DM 채팅 연출 (`dm.html`) | ✅ |
| 카드뉴스 편집기 (`tools/`) | ✅ |
| **실제 폰 QR 스캔 리허설** | ⚠️ **미확인 — 개막 전 반드시** |
| 다수 동시 접속 부하 | ⚠️ 미검증 |

이전 인수인계본에 «Supabase 테이블 생성 필요»라고 적혀 있었으나 **해소됐다.**
현재 `participants`·`answers` 테이블에 `미리보기`·`검수` 회차 데이터가 남아 있다.

---

## 2. 페이지와 주소

Vercel 정적 배포. `main`에 푸시하면 자동 재배포된다.

| 주소 | 화면 | 누가 |
| --- | --- | --- |
| `/` | 퀴즈 **무대 스크린** (복원율 게이지) | 진행자 — 극장 프로젝션 |
| `/?p=1` | 퀴즈 **관객 폰** | 관객 — QR 접속 |
| `/dm.html` | 덕이 ↔ 아란 **DM 채팅 연출** | 진행자 |
| `tools/카드텍스트편집기.html` | 인스타 **카드뉴스 제작** | 홍보 담당 |

무대 화면의 QR은 자동으로 `?p=1`을 인코딩하므로 관객은 스캔만 하면 된다.

### 공연 회차 분리 — 중요

주소 뒤 `?s=회차명` 으로 데이터를 나눈다. 붙이지 않으면 `live` 로 들어간다.
**무대 QR은 `s` 값을 관객에게 물려주지 않으므로**, 회차를 나눌 때는 관객용 QR을
따로 만들거나 공연 전에 데이터를 비운다(SQL은 [SETUP.md](SETUP.md)).

---

## 3. Q-Studio 연동 (구조상 가장 중요)

이 페이지는 **단독으로도 돌지만, 실제 공연에서는 Q-Studio(`F:\projects\Q-studio`)가
iframe으로 물고 큐로 띄운다.** 큐 이름은 `말모이 퀴즈 html`.

`index.html`은 자기가 임베드됐는지 스스로 판단해 동작을 바꾼다.

- `EMBEDDED` = `window.parent !== window` — iframe이면 참
  → **진행자 바·힌트 문구를 그리지 않는다.** "출력 화면에는 콘텐츠 말고 아무것도
    그리지 않는다"는 Q-Studio 원칙을 따른 것.
- `ON_STAGE` — 조종창의 «라이브 미러»가 같은 페이지를 한 벌 더 돌려 **소리가 두 번
  나는 문제**가 있었다. 부모가 `output.html`인 쪽(=진짜 무대)만 소리를 낸다.

> 임베드 상태에서는 진행자 바가 안 보이므로, **키 조작은 Q-Studio 쪽에서** 하거나
> 무대 페이지를 별도 창으로 직접 열어야 한다. 리허설 때 어느 쪽으로 운용할지
> 반드시 정해 둘 것.

---

## 4. 진행자 조작

### 퀴즈 무대 스크린

| 조작 | 동작 |
| --- | --- |
| **클릭 / Space / Enter** | 한 단계씩: 시작 → 순사 급습 → 작전 완료 → 리셋 |
| **1** | 의견 1건 수동 추가 — 게이지 **+1%** (말풍선·소리도 진짜 응답과 동일) |
| **7** | 순사 급습 — 70%로 떨어뜨리고 거기서 다시 상승 |
| **9** | 90% 고정 |
| **0** | 100% 고정 (완전 복원 연출) |
| **A** | 개입 해제 — 실집계 값으로 복귀 |
| **H** | 진행자 바 숨김/표시 |

> 이전 문서에 있던 **`E`(즉시 종료) 키는 현재 코드에 없다.** 종료는 클릭으로 한다.

### DM 채팅 (`/dm.html`)

| 조작 | 동작 |
| --- | --- |
| 클릭 / Space / → | 다음 대사 (타이핑 중이면 즉시 완성) |
| ← / Backspace | 뒤로 · **R** 처음 · **F** 전체화면 · **H** 힌트 숨김 |
| `+` `−` | 타이핑 속도 · `[` `]` 음량 |

효과음은 코드로 합성한다(외부 음원 없음). **첫 클릭 이후 소리가 켜진다** — 브라우저
정책이라 정상이며, 시작 클릭 한 번으로 해제된다.

---

## 5. 실시간 집계 구조

```
관객 폰 (/?p=1)  ──POST──▶  Supabase  ◀──1초 폴링──  무대 스크린 (/)
```

- 프로젝트 `mxsxkktgsmaumeugripe` · URL과 publishable key는 `index.html` 상단
- 테이블 `participants`(접속 기기) / `answers`(응답)
- 폰은 접속 시 기기ID를 등록하고, 문제마다 응답을 POST

**WebSocket이 아니라 폴링인 이유**: 공연은 단 한 번뿐이다. WebSocket은 이벤트가 한 번
유실되면 게이지가 영구히 틀어지지만, 폴링은 매번 총계를 다시 읽어 스스로 복구된다.

**게이지 계산**: 관객 수를 미리 알 수 없으므로 «목표 대비 비율»을 쓰지 않는다.
응답이 쌓일수록 상승폭이 줄며 90%에 수렴하는 곡선이다(30명이든 300명이든 거꾸로
가거나 멈추지 않는다). 주요 상수 — `MAX_PCT 90` · `DROP_PCT 70` · `HALF 50` ·
`BUBBLE_MAX 14` · `TOTAL 50`(시뮬레이션용).

---

## 6. 파일

| 파일 | 설명 |
| --- | --- |
| `index.html` | 퀴즈 본체. `?p=1` 유무로 무대/폰 두 모드 분기 |
| `dm.html` | DM 채팅 연출. 완전 독립 실행 |
| `landing.jpg` | 무대 로비 배경 |
| `tools/카드텍스트편집기.html` | 인스타 카드뉴스 제작 (아래 7항) |
| `qrcode.js` | QR 생성 (Kazuhiko Arase, MIT) |
| `build.js` → `malmoi-artifact.html` | Artifact 배포용 단일 파일. **웹 배포엔 안 씀** |

문서 — `README.md`(운영) · `SETUP.md`(Supabase) · `STAGE-SCREEN.md`(설계) ·
`CONTINUE.md`(개발 이어받기) · `TTS-VOICE.md`(음성 제작)

```bash
npx serve . -l 4321      # http://localhost:4321
node build.js            # malmoi-artifact.html 재생성
```

> `localhost`로 열면 QR도 `localhost`를 가리켜 관객 폰에서 안 열린다.
> QR 테스트는 반드시 배포 주소에서 할 것.

---

## 7. 인스타 카드뉴스 편집기

`tools/카드텍스트편집기.html` — 배경 이미지에 문구를 얹어 **1080×1350 PNG**로 내보낸다.
1탄 카드 4장의 문구가 프리셋으로 들어 있고, 카드 1번이 **국립한글박물관 카드**다.
간판에서 자음이 떨어져 나가는 「떨어진 조각 만들기」 기능도 있다.

주의할 점 두 가지.

- **배경은 `cover`라 가로형 원본은 좌우 40%가 잘린다.** 가운데 구도가 아니면 손실이 크다.
- **프리셋 좌표는 1탄 일러스트 기준**이다. 실사 사진에 쓰면 간판 글자 위치가 어긋나므로
  이미지마다 끌어서 다시 잡아야 한다.

검증용 이미지는 `tools/samples/`(git 제외). 원본은 `Downloads`와 `image-generator` 프로젝트에 있다.

### 홍보 원칙 — 스포일러 금지

관객이 **무엇을 하는지**(참여형·폰 사용)는 공개해도 되지만, **무슨 일이 벌어지는지**
(순사 급습, 게이지 하락, 결말, 실제 출제 문제)는 공개하지 않는다. 사투리 퀴즈를
콘텐츠로 쓸 경우 **공연에 나오지 않는 단어로만** 낸다.

---

## 8. 위험과 대응

- **네트워크 의존**: 극장 와이파이가 끊기면 집계가 멈춘다. 진행자 바에 «집계 끊김»이
  뜨므로 즉시 알 수 있고, 이때는 **1/7/9/0 키로 게이지를 수동 진행**하면 공연은 계속된다.
  진행자 개입 기능이 연출 도구이자 장애 대비책이다.
- **임베드 시 진행자 바가 안 보인다** — 3항 참고. 운용 방식을 미리 정할 것.
- **응답 조작 가능**: 익명 접속이라 반복 제출이 가능하다. 다만 게이지를 진행자가
  덮어쓸 수 있어 진행에는 지장이 없다.
- **이전 회차 데이터 혼입**: 현재 `미리보기`·`검수` 데이터가 남아 있다. 본공연 전
  `?s=` 로 회차를 나누거나 비울 것.
- **`malmoi-artifact.html`은 Supabase에 못 붙는다** — Artifact 환경이 외부 통신을 막는다.
- **publishable key는 공개가 정상**. 권한은 RLS가 통제한다.
  **`secret`(구 service_role) 키는 절대 넣지 말 것.**

---

## 9. 개막 전 체크리스트

1. **실제 폰으로 QR 스캔** → 무대의 접속·응답 숫자가 오르는지 (가장 중요, 미확인)
2. Q-Studio 임베드로 띄웠을 때 **소리가 두 번 나지 않는지**
3. 본공연 회차 `?s=` 값 확정, 이전 데이터 정리
4. 진행자 바를 프로젝션에 노출하지 않도록 `H` 동작 확인
5. DM 페이지 효과음 실제로 들어보기 (코드 합성이라 실측 미확인)
